// Dummy screen - not actually rendered
// The MS menu button opens a modal instead of navigating to this screen
import { View } from 'react-native';

export default function MSMenuScreen() {
  return <View />;
}
